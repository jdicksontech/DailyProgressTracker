# Progress Tracker

1. Install MySQL and Python requirements
2. Run `sql/setup_tables.sql` to create the database
3. Update `database connection` with your MySQL credentials
4. Run `python Progresscheck.py`
